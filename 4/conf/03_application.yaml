apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: app2
  namespace: demo-acm-4
spec:
  selector:
    matchExpressions:
      - key: app  
        operator: In
        values:
        - app2
  componentKinds:
  - group: apps.open-cluster-management.io/v1
    kind: Subscription
