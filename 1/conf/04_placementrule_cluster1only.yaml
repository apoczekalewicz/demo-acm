apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  labels:
    app: httpd
  name: cluster1only
  namespace: demo-acm
spec:
  clusterSelector:
    matchLabels:
      clusterid: cluster1
